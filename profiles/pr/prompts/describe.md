Read the PR at {{PR_LINK}} and update its title and description in a way that describes the nature of the changes from a broader standpoint. Take the larger project into account (microservice-level), the changes it implies in other scopes (dependencies, new constraints, new capabilities, breaking changes), and the high-level functional choices that were made. I don't care about code internals such as classes, data structures, etc. The description of the PR should help a human reviewer to make sense of the PR even if it contains a lot of changes, so keep it short and to the point, high-value information only. Don't include a 'Summary' header in the PR description. If you see commit messages that do not fit the standard format (expected format: fix/feat/docs/cleanup/chore(<service name>): short commit description), rewrite them without changing the commit content.{{EXTRA_INSTRUCTIONS}}
