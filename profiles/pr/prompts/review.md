See the review comments on {{PR_LINK}} that are not closed (if they are not there yet, check if a Copilot review is pending and wait until it's done). Address the review comments in a single commit named 'fix(<service name>): apply Copilot review' and then push + reply to each comment with a useful short sentence. If you see failing CI checks, fix them in a second commit named 'fix(<service name>): fix CI'. Confirm your changes fixed the CI and that all review comments were addressed. Don't co-author the commits. Work in a worktree to not alter the current git repo. If you see older commit messages that do not fit the standard format, rewrite them without changing the commit content. Push and delete the worktree when you are done.{{EXTRA_INSTRUCTIONS}}
